{
  "name": "agent-fit",
  "version": "1.0.0",
  "description": "AI agent mission app",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build": "cd backend && npm install && npm run build",
    "start": "cd backend && npm start"
  }
}
